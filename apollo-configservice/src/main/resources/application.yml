spring:
  application:
    name: apollo-configservice
  profiles:
    active: ${apollo_profile}

ctrip:
  appid: 100003171

server:
  port: 30080

logging:
  file: /usr/local/data/apollo/logs/apollo-configservice.log
